{% extends 'base.html.twig' %}

{% block title %}Hello{% endblock %}

{% block body %}

    {% for category in tableau %}
    <h1>{{ category.owner.name }}</h1>

        {% for article in category %}
            <h3>{{ article.title }}</h3>
            <p>{{ article.content }}</p>
        {% endfor %}

    {% endfor %}



{% endblock %}
